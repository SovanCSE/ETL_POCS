
----- Need to run the following query for historical load
TRUNCATE TABLE SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY;
REMOVE @SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY_NAMED_STAGE;
TRUNCATE TABLE SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY_SCD2;
TRUNCATE TABLE SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY_SCD1;


LS @SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY_NAMED_STAGE;

SELECT $1, $2, $3,$4,$5,$6, metadata$filename FROM @TRAVEL_HISTORY_NAMED_STAGE;

SELECT * FROM SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY;

SELECT * FROM SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY_STREAM;

SELECT * FROM SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY_SCD2 
ORDER BY PERSONID, TRAVELDATE DESC;

SELECT * FROM SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY_SCD2_STREAM;

SELECT * FROM SAMPLE_DB.SAMPLE_SCHEMA.TRAVEL_HISTORY_SCD1 
ORDER BY PERSONID, TRAVELDATE DESC;
